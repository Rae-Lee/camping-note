version: '3.8'
services:
   web:
        build:
            context: .
            dockerfile: Dockerfile
        env_file: .env    
        ports:
            - "3000:3000"   
        environment:
            CLEANUP_ONDESTROY: true
        volumes:
          - ./:/app  
          - /app/node_modules  